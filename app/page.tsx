// Phase 1 placeholder. Replaced in Phase 4 (Homepage).
// Exists so `next build` produces a `/` route and Phase 1 DoD can pass.

export default function HomePage() {
  return (
    <div>
      <h1>Astons Law Chambers</h1>
      <p>Phase 1 scaffold placeholder. The homepage is built in Phase 4.</p>
    </div>
  )
}
